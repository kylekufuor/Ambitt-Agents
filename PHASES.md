# Ambitt Agents — Build Phases
Last updated: 2026-05-19 (Atlas onboarding — Phase 1 SHIPPED + E2E-verified; **proposal-email JSON-contract + Handlebars render pipeline SHIPPED + E2E-verified `561fc19`** — Atlas emits `ProposalEmailData` JSON → Handlebars renders, zero-retry pass; Gmail render bugs found [strips inline SVG; agent-name leaked into greeting/signature] + a proposed pivot to an email-teaser + hosted `/proposals/[token]` page awaiting Kyle's go; **Phase 2a SHIPPED `e0e7626`** — onboarding form rebuilt as a 9-slide chapter slideshow per Kyle's mockup + generic placeholders / Email·Slack·WhatsApp channels `4644b0d`; 5-change handoff #1/#2 + #4 SOP-upload UI + #5 multi-select checkpill chips SHIPPED `4d274cb`, target-audience chips added `2e270c1`, CSS-specificity bug fixed & Enter-to-continue added-then-removed `8972943`→`e33decb`, only #3 Composio-autocomplete tools still open; post-2a polish `c29db32`→`b85d770` — welcome-slide magazine TOC, `agentName`/`agentRole` fields + 12-chip role selector, synchronous "proposal incoming" ack email before the Atlas run, and **cadence scope clarified: the onboarding form only captures a Scheduled-vs-Triggered binary — the actual cron is portal-configured post-launch, not collected at intake**; see project_onboarding_agent_scaffold.md)

Full scope decisions documented in `.claude/projects/-Users-kylekufuor-Projects-Ambitt-Agents/memory/project_client_interaction_scope.md`

---

## Phase 1 — Foundation (must have before any client) — COMPLETE

- [x] Revised pricing model in code — `shared/pricing.ts` is source of truth. Tiers: Starter $499 (1K, 1 agent), Growth $999 (3K, 2 agents), Scale $2,499 (10K, 3 agents). Tool limit removed. Overage always on at tier-specific rates ($0.60 / $0.40 / $0.30). Second-agent 20% discount. `OverageEvent` table rolls up for end-of-month billing.
- [x] Onboarding flow — full sequence built. T+0 welcome email with AI-authored strategic brief (PDF attached) + T+5min "how to work with me" (AI-personalized) + T+3 check-in + T+7 capability highlight + T+14 feedback + 2nd-agent pitch. All AI-generated via `oracle/onboarding-content.ts`. Non-billable (`runAgent({ billable: false })`). Cancelled on pause/reject/kill. Hourly cron in `oracle/scheduler.ts` respects agent timezone + 9-5 M-F business hours. `preferredName` field used for greetings.
- [x] Email footer with persistent navigation links in all 10 templates (`_shared.ts::navFooterLinks`, `footerBlock(agentName, agentId, { systemEmail })`)
- [x] Interaction counter — tracked per agent (`Agent.interactionCount`/`interactionLimit`/`overageCount`). Runtime enforcement live (`shared/runtime/engine.ts`), monthly reset via `interactionResetAt`. No UI surface yet — admin-dashboard and client-portal surfacing both land in Phase 2 ("Client portal expansion"); data exists, UI home is part of that build.
- [x] Operator dashboard panels — `dashboard/src/lib/health.ts` — agent error rate panel on `/agents/[id]`, client engagement + churn signal on `/clients/[id]`, churn-risk badges on `/clients` list. Costs page covers per-client API cost / revenue / margin / MRR from earlier work.
- [x] Prompt caching — `cache_control: ephemeral` on system prompt + tool definitions. Per-run cache metrics logged (`cacheCreationTokens` / `cacheReadTokens`). Cost model in `dashboard/src/lib/costs.ts::recalcCostCents` factors 1.25× write / 0.10× read rates.
- [x] Haiku triage routing — escalation pattern: Haiku 4.5 drives tool-selection loops; on end_turn with tools used, escalates to Sonnet for synthesis. Kill switch: `DISABLE_TRIAGE_ROUTING=1`. Per-model usage logged as separate `ApiUsage` rows, with `isPrimaryRun` flag so run-count metrics don't double.

## Phase 2 — Client interaction

- [x] Client portal expansion — agent detail page at `/agents/[id]` (pause/resume, schedule editor, interaction counter with progress bar, doc upload moved here), billing view on home (MRR + cycle usage aggregated across agents + overage cost from `OverageEvent`), "Voice & email" section (tone editor wires into `prompt-assembler`, `emailFrequency` stored+editable; digest options UI-disabled until pipeline built). Ownership enforced via portal proxy routes + Supabase session.
- [x] "Request new tool" form in client portal — `ToolRequest` table, submit form on agent detail page, WhatsApp ping to Kyle on submit, previous-requests list with status.
- [x] Chat page (chat.ambitt.agency) — lightweight web UI, token-based auth, unified conversation history
- [x] Oracle HTTP POST endpoint for chat (mirrors inbound-email flow, triggered by chat instead of Resend webhook)
- [x] `channel` field on ConversationMessage ("email" | "chat")
- [~] Tool connection flow — agent requests a missing tool mid-run via the `request_tool_connection` platform tool, which sends the client a Composio OAuth Connect Link.
  - [x] `ToolConnectionRequest` table (dedupes 24h window on `(clientId, appName)`)
  - [x] `shared/platform-tools/request-tool-connection.ts` handler (5 outcome states: emailed / already_pending / already_connected / unavailable / error; verified via `scripts/test-request-tool-connection.ts` against real Composio + real DB)
  - [x] `permission-email.ts` honors `ctaUrl` (previously dead prop; now drives the "Grant Access" button to the OAuth URL)
  - [x] Engine wiring — new built-in tool exposed to every agent via `BUILTIN_CLAUDE_TOOLS` in `shared/runtime/engine.ts`; `AgentContext` carries `clientId` + `clientName` so the permission-email payload satisfies `BaseEmailProps`
  - [x] Composio callback reconciliation — `/composio/callback` looks up `ToolConnectionRequest` by `composioConnectionId`, verifies with Composio (`getConnectedAccounts`) that the connection is ACTIVE, then flips `status="connected"` + `connectedAt`. Forged callbacks can't flip rows without a real active connection at Composio.
  - [x] Deploy Oracle — live as of 2026-04-19 (`df192477`); callback endpoint smoke-tested in prod (no-param + unknown-id both 200, correct log lines fire)
  - [x] `already_connected` branch verified in prod against real Composio (supabase/posthog)
  - [~] Happy-path E2E click-through — **deferred 2026-04-19**. Only unknown is which query param name Composio uses on its callback redirect; `/composio/callback` accepts four common names as a guess. If wrong, Oracle logs immediately flag it (one-line fix). Worst case first paying client's row sits at `status="emailed"` until the log surfaces. Probe is `scripts/test-tool-connection-live.ts`.
- [x] Email digest pipeline — `daily_digest` / `weekly_digest` live. Per-run output routes through `oracle/lib/dispatchAgentResponse.ts`: immediate sends email now, non-immediate queues a `ScheduledEmail(kind="digest_pending")` with a JSON payload of the run. Hourly `processDueDigests` cron (in `oracle/lib/digestCron.ts`) rolls up pending rows into one `digest-email.ts` send per agent at `digestHour` (and `digestDayOfWeek` for weekly) in agent timezone, with a Sonnet-synthesized 1-2 sentence summary. Portal config editor exposes frequency + hour + day pickers. Attachments dropped in digest mode (metadata kept for counts).

## Phase 3 — Advanced capabilities

- [~] Browser cluster — sliced into Phase A (public-web browser tool), Phase B (1Password plumbing), Phase C (secret injection bridge). Tool shape decision: one big `browse(goal, starting_url?)` tool calling Stagehand's `agent()` internally (not three granular tools) — Stagehand already does multi-model routing + fewer tokens per task.
  - [x] **Phase A — public-web browser tool** (deployed)
    - [x] Step 1: scaffold — `@browserbasehq/stagehand@3.2.1` installed, `BrowserSession` model live on prod DB (audit row per run; schema mirrored across root/client-portal/dashboard), type-check clean (`c28cd10`).
    - [x] Step 2: `shared/platform-tools/browser.ts` — `runBrowserTask({agentId, clientId, goal, startingUrl?})`, 5-min Promise.race timeout, Stagehand `agent.execute()` with `anthropic/claude-sonnet-4-5-20250929` (4-6 hits AI_NoObjectGeneratedError), `startingUrl` folded into instruction (avoiding Stagehand's brittle gateway-routed `act()`). Logs `BrowserSession`. Registered in `BUILTIN_CLAUDE_TOOLS` + engine executor branch. System-prompt `BROWSER_RULES` section: when to browse vs web_search, side-effect approval rule for supervised mode.
    - [x] Step 3: local probe verified end-to-end against real Browserbase + real DB. example.com goal → success in 13s, 3 actions, "Example Domain" extracted, BrowserSession row populated correctly. `scripts/test-browser-probe.ts` is the probe.
  - [x] **Phase B** — 1Password SDK plumbing landed, deployed, and verified end-to-end (`9a9e577` + `b7d293b` on origin/main). `Client.onepasswordVaultId` column live on prod DB; `@1password/sdk@0.4.0`; `shared/secrets/onepassword.ts` exports `resolveSecret(clientId, ref)` + `resolveSecrets(clientId, refs[])`, both gating the ref's vault against `Client.onepasswordVaultId` (one Ambitt service account, read access per client vault). `OP_SERVICE_ACCOUNT_TOKEN` live on Railway Oracle (read-only service account "Ambitt Oracle"); Kyle's Client row pinned to vault NAME `"Ambitt-Kyle"` — NB the column stores the vault *name*, not the URL UUID (the SDK rejects UUID-form `op://` refs). Live probe `scripts/test-op-resolver.ts`: all 5 checks pass (parser, happy path with no value-leak in logs, vault-mismatch rejected pre-SDK, no-vault rejected, batch). No consumer path yet — Phase C wires it in.
  - [x] **Phase C** — secret injection bridge committed (`d5bdba4`): `browse` detects `{{secret:op://vault/item/field}}`, resolves via Phase B, tokenizes before handing to Stagehand, CDP-level interceptor swaps token→value just before keystrokes. Companion `request_credential()` platform tool (`05fcd3d`) provisions an empty 1Password item via `items.create()` and emails it; dedicated `credential-request-email.ts` template shipped (`4488f60` → conversational redesign `66191da`, the first archetype-A email). **Credential-collection rework (build in progress — 5 of 8 steps shipped + deployed):** the client-facing 1Password deep-link is a blocker (clients don't have 1Password / would have to sign into Ambitt's vault), and the interim HMAC-token-form idea was dropped too. Final design: a **portal Tools page** at `clients.ambitt.agency/agents/[id]/tools` is the single source of truth for an agent's tool/credential config — Supabase-session auth (no signed tokens, no expiry/replay). Two sections: **Tools** (derived view: Composio connected accounts + `ToolConnectionRequest` rows + 1P vault items + credential-request `Recommendation` rows; per-row OAuth "Connect" / inline "Add credentials" form / both-stacked for dual-mode tools like LinkedIn; logos from Composio catalog; per-row audit trail = last-accessed time + count from the new `CredentialAccess` table) and **Personal info** (custom non-tool credentials — SSN etc. — under a `Personal-Info` 1P section). `request_credential()` now just creates the empty 1P item + emails a *notification* deep-linking the portal row. `🔒 Secured with 1Password` trust badge on form/row/email; credential value transits Oracle process memory only during the submit POST, never DB/logs (planned guard probe). Dropped: standalone `/credentials/<token>` page, credential-token signer, expiry handling. 8 steps: (1) ✅ `CredentialAccess` audit table (`clientId, agentId, vaultName, itemTitle, field, accessedAt`) mirrored across 3 prisma schemas + pushed to prod (`4d8fe7b`); (2) ✅ audit-row write in `resolveSecret(clientId, ref, agentId?)` (best-effort, never blocks resolve) + `populateItem(clientId, itemId, fieldValues)` write helper in `shared/secrets/onepassword.ts` (`items.put()`), E2E-verified vs real 1P+DB by `scripts/test-op-populate-and-audit.ts` (`80bee77`); (3) ✅ Oracle `GET /agents/:id/tools` — derived view merging Composio connected accounts + app catalog (logos/names) + 1P vault items (`listVaultItems(clientId)` → `VaultItemSummary[]` with per-field `{title, fieldType, filled}`) + `CredentialAccess` last-accessed; returns `tools` + `personalInfo` arrays (`3c79658`); (4) ✅ Oracle `POST /agents/:id/tools/credentials/:itemId` — `populateItem` + marks matching pending `credential-request` Recommendation approved/resolved; auth delegated to portal proxy (`8a63cdf`); (5) ✅ portal `/agents/[id]/tools` server page (ownership via `verifyAgentOwnership` Supabase session) + `tools-list.tsx` client component (Tools section = Composio-matched rows with status badge + OAuth/credentials buttons; Personal-info section = 1P-only rows; inline expand-to-edit form, show/hide on concealed fields; `🔒 Secured with 1Password` tag) + Next proxy routes under `client-portal/src/app/api/agents/[id]/tools/...` (`5c6a26e`). Both Oracle + Client Portal deployed to Railway. (6) expandable-rows / dual-mode-stacked-buttons polish (largely folded into step 5); (7) email deep-link updates (`request_credential` + `request_tool_connection` → portal row); (8) E2E smoke test (LinkedIn — the only job-applier-specific piece; the rest is generic platform infra). Phase B `resolveSecret()` real-credential round-trip happens via the portal form. NB the client portal uses client-side Supabase auth + `verifyAgentOwnership` — Kyle's operator email can't view a client's Tools page; must sign in as the owning client (Atlas is owned by `mcquizzyapp@gmail.com`).
- [ ] Custom email domain support — Resend multi-domain, subdomain approach, DNS verification flow
- [ ] Job-applier agent (client #0) — Kyle's own job-application agent, code-scaffolded (`scripts/seed-job-applier.ts`), as the platform's v1 validation case. Full plan + 11-step build queue in `.claude/.../memory/project_job_applier_agent.md`. Sequence: Phase B deploy (done) → real-vault resolver probe (done, `b7d293b`) → `request_credential()` tool (done, `05fcd3d`; service-account Read+Write rotation complete; live-tested — created `op://Ambitt-Kyle/LinkedIn`) → Phase C secret-injection bridge (done, `d5bdba4`) → `browse` file upload → resume storage → application tracking via `Recommendation` → one-click web actions (`/actions/:id/:verb?t=`) → job-card email template → system prompt + seed script → E2E apply to a real Greenhouse/Lever posting. Open: scope-before-client-#1 (≈8 must-haves from the 19-item "Marcus scenario" checklist vs. build all 19) — Kyle hasn't decided. Also: a system-prompt rule preferring `request_tool_connection` (OAuth) over `request_credential` (1Password) when an API covers the task (LinkedIn job-apply has no API → browser-as-you is the only path). (The dedicated `credential-request` email template is done — `66191da`; the credential *collection flow* itself is being reworked, see Phase C.)
- [x] Autonomy modes — `autonomyLevel` narrowed to `"supervised" | "autonomous"` (legacy advisory/copilot map to supervised). Supervised gate built end-to-end: `request_approval` platform tool (`shared/platform-tools/request-approval.ts`) creates a `Recommendation` row + fires `action-required` email; engine pauses on `isPause` signal and embeds the plan into conversation history so resume sees it. APPROVE/RETRY/DISMISS subject replies in `oracle/index.ts` resume the agent run. Autonomy section in `prompt-assembler.ts` instructs Claude when to call the tool. Portal `ConfigEditor` exposes the toggle; Oracle `/agents/:id/config` validates the field. Atlas + Zay migrated `advisory → supervised` in prod (commits `1cc40ea`, `c2c9481`).
- [x] Proactive insights — system prompt teaches Claude to surface actionable/relevant/non-obvious observations at the end of any response as a trailing `## Proactive insights` bullet list. `dispatchAgentResponse.ts::extractProactiveInsights` parses the trailing section out of response text and passes it to `agent-response.ts` as `proactiveInsights: string[]`, which renders a highlighted amber block above the reply-prompt. 1-3 bullets max, empty sections suppressed entirely. In supervised mode, action-implying insights flow through `request_approval` like any other side-effectful work.

## Phase 4 — Voice + multi-agent

- [ ] Retell AI integration — WebSocket endpoint on Oracle bridging to `runAgent()`
- [ ] Voice model routing — voice calls must use Sonnet (sub-1s latency requirement)
- [ ] Phone number provisioning per agent
- [ ] Multi-agent pricing — 20% discount on second agent
- [ ] Voice channel added to email footer and client portal

---

## Key decisions (locked)

- **No tool limit.** Unlimited tools across all tiers. Gated by interaction volume.
- **Pricing (source of truth — see `shared/pricing.ts`):** Starter $499/mo (1K interactions, 1 agent), Growth $999/mo (3K, 2 agents), Scale $2,499/mo (10K, 3 agents). Setup $1,000–2,500. Annual = 10 months billed (2 free).
- **Overage (always on, flat per tier):** Starter $0.60/interaction, Growth $0.40, Scale $0.30. Matches ~20% premium over included per-interaction rate.
- **Second agent:** 20% discount off tier price (applies to 2nd+ agent on Growth and Scale).
- **Interaction limit:** agent keeps running and charges overage per extra interaction (no hard stop).
- **Tool connections:** platform tools (zero friction) → OAuth via Composio Connect Link (30 sec) → white-glove call (rare).
- **Chat + email = one conversation.** Unified history, `channel` field differentiates.
- **Agent always mirrors back** what it understood before acting (both modes).
- **Approve/reject is entire action**, never partial. Client modifies via natural language reply.
- **Proactive insights** only when actionable and relevant. No noise.
- **Email footer** on every email with all navigation links.
- **Custom email domain** is Growth/Scale feature (subdomain approach).
- **Observability built in-house** — no third-party analytics tools.
- **Browser automation:** Browserbase + Stagehand. Credentials never touch the LLM.
- **Voice:** Retell AI with custom LLM (Claude as the brain).
- **Email design:** ~31 templates across an agent's lifecycle, grouped into 5 visual archetypes — Interactive Setup / Data Digest / Decision Request / Status Notification / Admin-Lifecycle. Conversational warm-light house style replaces the dark-bar `_shared.ts` system look; first instance is `credential-request-email.ts` (`66191da`). Build the design primitives once per archetype, then batch-port templates into it. Full catalog: `.claude/.../memory/project_email_templates_catalog.md`.
- **Onboarding agent + scaffold (2026-05-18 pivot, Phase 1 SHIPPED 2026-05-18):** agent creation moves to a self-serve **onboarding agent** (link → SOP upload + discovery Qs → Claude artifact spec for client refinement → Kyle-approved **hybrid** quote → Kyle builds on the existing infra). **Admin dashboard UI is FROZEN** (Kyle-only; Oracle ops via CLI/DB/scripts; polish later). Client portal stays (billing + config + tools/creds). Build approach (a) — prompt + Composio/`browse` tools + schedule + ≤1 custom platform tool — covers ~80–90% of "custom" requests; full per-agent codebase only for heavy data pipelines / branching multi-day stateful workflows / bespoke domain math. Flow now locked: one reusable hosted web form (hard-data fields → SOP upload → SOP-aware chat) → Claude-built **presentation** (sales artifact, **no price**) emailed with Approve / Make-changes (pre-filled form + comments → regen) → Kyle-reviewed **quote = setup fee + monthly retainer** → **Prospect** (separate DB entity) converts to Client on accept+pay → portal **status page** through build → launch (retainer meter starts). Build-assist starts blank-slate-from-spec; auto-draft `pending_approval` agent later. The onboarding agent is named **Atlas** (`atlas@ambitt.agency`) — Ambitt's own internal-client agent / platform dogfood; pre-existing McQuizzy "Atlas" (`cmnkvvtsf0002lz6xkloh21y0`) deleted. **Phase 1 shipped + E2E-verified 2026-05-19** (`39a4d20` → `9da8459`): (i) `Prospect` model live on prod (status workflow `discovery → discovery_complete → presentation_sent → revising → quote_pending → quote_sent → accepted → ghosted/archived`, JSON `formData`, `convertedClientId` FK); (ii) `Agent.acceptFromProspects` flag; (iii) `scripts/seed-atlas.ts` — Ambitt Client `cmpbu3pio000024xsa180ykbt` + Atlas Agent `cmpbu3pta000224xsc8i0esnm`; (iv) **inbound sender-auth** in `oracle/index.ts::checkInboundAuth` (sender must equal `agent.client.email` else 200 ignored + warn-log; Atlas-style platform agents match against active `Prospect.email` instead — future `Client.authorizedSenders[]` is a `string[]` extension on the same check); (v) `clients.ambitt.agency/onboard/[token]` token-gated form (middleware bypasses Supabase, server validates token, locked 13-field section schema, paste-text SOPs); (vi) Oracle `POST /onboarding/prospects/:id/event` web→runtime bridge — for `form_submitted`, builds Atlas-shaped message from intake, runs `processInboundMessage(atlasId, msg, billable:false)`, saves response to `Prospect.presentationHtml`, status → `presentation_sent`, emails via inline `wrapPresentationEmail()` with Approve (mailto) / Make-changes (URL back to form). Portal submit is fire-and-forget; Atlas runs async (real submissions take ~3+ min — Atlas does genuine `site_scan` + ~97s `browse` research, validating the don't-make-the-browser-wait design; refuses to fabricate from an empty form). Stale Oracle URL fallback (`ambitt-agents-production`, dead) corrected across portal in 4 files; `ORACLE_URL` env now set on Client Portal. **E2E-verified 2026-05-19** — found+fixed two gaps: middleware bypassed only the `/onboard/**` page not the `/api/onboard/**` route (submit 307'd to `/login`, `cf48600`); Atlas runtime response leaked a Claude conversational preamble before the `<div>`, now sliced in `wrapPresentationEmail()` (`9da8459`). **Post-Phase-1 shipped 2026-05-19:** intro-copy refinement done + onboard form/presentation-email restyled to the AmbittMedia brand palette — primary teal `#00b3b3` (`#099` hover), Geist, neutral palette pulled from ambittmedia.com; placeholder "A" badge until Kyle sends the logo (`bca347a`). Client-facing voice scrub: speak as "we"/"our team", never name Kyle; Atlas taught to write like a human, not AI (`3acc4e8`). Phase 2 queue: **slide-form UX SHIPPED 2026-05-19 Phase 2a `e0e7626`** (single-page form → 9-slide chapter slideshow built to Kyle's mockup; + generic placeholders / Email·Slack·WhatsApp channels `4644b0d`; open onboard-form follow-ups from Kyle's 5-change handoff — #3 Composio-autocomplete tools field, #4 no-storage extract-then-discard SOP file upload, #5 multi-select checkpill chips [absent from mockup, labels TBD from Kyle]), SOP-aware chat post-form, `requested_changes` regen handler, real Approve action (currently mailto dead-end), **Vera** (Kyle 2026-05-19 — LOCKED+specced, not built: editor/QA sister agent to Atlas, owned by Ambitt; Atlas calls `request_review(...)` before send, Vera returns approve or issues/suggestions, Atlas iterates ≤3× (refined 2026-05-19: Vera reviews the structured `ProposalEmailData` JSON, not rendered HTML — cheaper field-level checks); v1 checks HTML hygiene + required sections + no forbidden content + specificity + brand-voice; Haiku 4.5; scope v1 = presentation, later all client-facing email; Task #31), **proposal-email JSON-contract + Handlebars pipeline** (Kyle 2026-05-19 — **SHIPPED + E2E-verified `561fc19`**: Atlas emits `ProposalEmailData` JSON → Handlebars `oracle/templates/proposal-email/template.html` renders HTML → send, replacing inline `wrapPresentationEmail()`; single-pass JSON + Zod validate + retry-once-on-same-thread; new `Prospect.presentationData Json?` column [3 schema mirrors + db push] stores the JSON for Vera/audit; E2E run = clean JSON zero retries. Open Gmail render bugs: Gmail strips inline `<svg>` [hero icon → swap to hosted `<img>`], Atlas leaked the agent name into `greeting.name` + the sample-email signature [prompt-tighten], agent-name inconsistent across sections. **Proposed (not approved):** abandon email-as-artifact → email teaser + hosted `clients.ambitt.agency/proposals/[token]` page reusing the same JSON+template, PDF download Phase 3 — because email clients permanently mangle the rich render; decide before more Atlas email work), **structured form options** (dropdowns/checkboxes for industry/role/agent-category/volume/output-format, married to slide-form — Claude proposal, not yet locked). **Phase 2 build-kickoff — PRD generation Phase 0 + A SHIPPED + E2E-verified 2026-05-22 (`5772b12` → `c7527ae` → `c590347`):** on scope-approval (`quote_pending`) the approve route fires `POST /onboarding/prospects/:id/generate-prd` in parallel with the `scope_approved` ops-email; Atlas re-runs the form data → emits `AgentPRDData` JSON (Zod + retry-once) → stored in `Prospect.prdData`/`prdGeneratedAt` → Kyle reviews on dashboard page `prospects/[id]/prd` (iframe to Oracle `GET /prd-html`) with Approve / Regenerate-with-notes. Zod failures now 422-with-message (not 500); pricing grounded in `web_search` market research with a "Pricing receipts" section. At-payment auto-scaffold (Phase B) still unbuilt. Full detail + commit refs: `.claude/.../memory/project_onboarding_agent_scaffold.md`.
- **Credential management:** 1Password SDK for non-OAuth credentials — but 1Password is *invisible backend storage*, never a client-facing UX. Clients manage credentials + tool connections from the **portal Tools page** (`clients.ambitt.agency/agents/[id]/tools`, Supabase-session auth) — inline credential forms, per-row audit trail, `🔒 Secured with 1Password` badge; the value transits Oracle only during the form POST, never persisted in our DB/logs. (Flow being built — 5 of 8 steps shipped + deployed; remaining: dual-mode polish, email deep-link redirect, E2E smoke test.)
